# RediDocker
# Minimal Dockerfile to build a Redis image for local use.
# Named `RediDocker` per request (if you prefer the standard name use `Dockerfile`).

FROM redis:7-alpine

LABEL org.opencontainers.image.title="sales-agent-redis"
LABEL org.opencontainers.image.description="Minimal Redis image for the sales-agent project (based on redis:7-alpine)."

# Expose Redis default port
EXPOSE 6379

# By default use the image's default command (redis-server). This keeps the image
# behavior consistent with the official Redis image. If you want to provide a custom
# redis.conf, copy it into the image and change the CMD accordingly.
CMD ["redis-server"]
